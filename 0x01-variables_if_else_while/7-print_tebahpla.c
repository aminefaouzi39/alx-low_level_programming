#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	while (n = 122 ; n >= 97; ch--)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
